# Local editor / tooling
.claude/
.vercel/
node_modules/

# OS cruft
Thumbs.db
.DS_Store

# NOTE: admin/config.js is committed on purpose — it holds only the PUBLIC
# Supabase URL + anon key. The service_role secret must never enter this repo.
